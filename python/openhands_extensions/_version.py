"""Package version.

``release-please`` bumps this together with ``package.json`` (see
``release-please-config.json`` -> ``extra-files``), and
``tests/test_version_alignment.py`` asserts the two never drift apart.
"""

__version__ = "0.5.0"
