# Example Tasks

- “Connect DealWatch to OpenHands and check whether the runtime is ready.”
- “Compare these grocery URLs before creating any watch task.”
- “Show me which read-only DealWatch tool I should call first.”
- “Inspect one watch group without mutating any state.”
