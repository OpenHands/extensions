# Example Output Shape

```text
Current story: The selected watchlist story is still centered on the same claim, but two new evidence runs changed the confidence mix.
What changed: One new artifact was added and one previous source fell out of the latest compare window.
Evidence used: current briefing payload, latest compare summary, and one artifact lookup.
Suggested next action: Re-run the retrieval/search leg for the selected watchlist before sending the next operator briefing.
Runtime gap: MCP was unavailable, so this answer used the local HTTP API only.
```
