{
  "id": "github-pr-reviewer",
  "name": "GitHub PR review copilot",
  "category": "Code review",
  "description": "Watch pull requests, inspect the diff, and leave a concise review with risks and suggested follow-ups.",
  "requiredIntegrationIds": [
    "github"
  ],
  "popularityRank": 100,
  "estimatedSetupMinutes": 4,
  "prompt": "Create an automation that reviews GitHub pull requests when they are opened or updated. Prefer a GitHub event trigger/webhook. If the user needs a local cron polling fallback, implement it as a custom script that first polls the GitHub API and checks persisted state, then only starts an OpenHands conversation when a new or updated PR actually matches the configured repository/filter. Use the GitHub MCP or API to inspect the PR diff, changed files, tests, and existing discussion. Post a short review that highlights correctness risks, security issues, missing tests, and concrete next steps. Ask me for the repositories, filters, review tone, and whether comments should be posted automatically or drafted first.",
  "exampleImplementation": "Preferred trigger: github.pull_request.opened and github.pull_request.synchronize\nLocal polling fallback: custom cron script, see automations/examples/github-pr-reviewer-polling/main.py\nRequired MCP: GitHub\n\n1. Prefer webhook/event delivery so the agent only runs on real PR events.\n2. If local polling is required, use a custom script that polls GitHub for changed PRs since the last stored timestamp and exits after the completion callback when there are no matching PRs.\n3. Only after a PR matches the repository/filter/state gate, start an OpenHands conversation for that PR.\n4. Read repository, pull_request, and sender from the event payload or polled PR record.\n5. Fetch the PR diff and related comments through the GitHub MCP/API.\n6. Run a review rubric focused on bugs, security, tests, and maintainability.\n7. TODO: decide whether to publish inline comments, a summary comment, or a draft artifact.\n8. Post or save the review with links back to the exact files and lines."
}
